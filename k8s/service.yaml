apiVersion: v1
kind: Service
metadata:
  name: customer-app
  labels:
    app: customer-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: customer-app
